{
  "name": "crateDIGGAOG",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy-extension-files",
    "copy-extension-files": "cp manifest.json dist/ && cp cratediggerDB.csv dist/ && cp -r icons dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.155.0",
    "papaparse": "^5.4.1"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
